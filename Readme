WorkerManager.cpp:
Modified:
void WorkerManager::handleIdleWorkers() 
	if 2 miners, send idle worker to combat
WorkerManager::finishedWithCombatWorkers()
	finished combat workers set 
WorkerManager::finishedWithWorker(BWAPI::Unit unit) 
	not finish combat worker

Add:
	BWAPI::Unit WorkerManager::getWorkerCombat()
	return worker if its job is combat


ProductionManager:
Nodified:
	ProductionManager::update() 
		if more than 4 workers, bunker less than two, and any unit is under attack, add bunker as highest priority to build order
	BuildingManager::getBuildingLocation(const Building & b)